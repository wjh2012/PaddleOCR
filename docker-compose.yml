services:
  ppocr:
    image: paddlepaddle/paddle:latest-dev-cuda10.2-cudnn7.6-trt7.0-gcc8.2
    container_name: ppocr
    ipc: host
    network_mode: "bridge"
    stdin_open: true
    tty: true
    volumes:
      - .:/paddle_custom
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
